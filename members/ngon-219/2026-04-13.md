# Một công ty đang triển khai một hệ thống lưu trữ chia sẻ cho ứng dụng gaming, được hosted tại trung tâm dữ liệu tại chỗ. Công ty cần khả năng sử dụng Lustre client để truy cập dữ liệu. Giải pháp phải quản lí toàn bộ. Đâu là solution đáp ứng đc
> Suy nghĩ yêu cầu lưu trữ game nên cần tốc độ, hiệu suất cao, fully managed. Tạo một Amazon FSx for Lustrue file system. Thêm gán file đến origin server, kết nối app server đến file system. Amazon FSx là một dịch vụ fully managed bơi AWS, cung cấp hệ thống hiệu năng cực cao cho tệp hệ thống, tối ưu hóa cho các tác vụ cần xủ lý nhanh như gaming và HPC. Hỗ trợ Lustre Client, cho phép máy chủ on-premises kết nối thông qua kết nối AWS Direct Connect hoặc AWS VPN

> Đáp án A dùng AWS Storage Gateway sai vì nó chỉ hỗ trợ giao thức lưu trữ tệp chuẩn NFS và SMB

> B sai vì EC2 không phải fully managed

> C dùng EFS, khiến trúc được xây dựa trên giao thức NFS, khôn thể cấu hình nó hỗ trợ Lustre

# Một công ty muốn xây dựng hạ tầng quản lí key có thể scale để hỗ trợ dev người cần mã hóa data vào ứng dụng của họ, đâu là solution đưa ra để giảm thiểu gánh nặng vận hành?

> Suy nghĩ đơn giản là dùng MFA để bảo vệ encryption key (sai vì MFA xây dựng chỉ là biện pháp bảo mật truy cập cho IAM user, không phải dịch vụ quản lý mã hóa)

> Đúng sẽ dùn AWS Key Management Service để bảo vệ encrytion key (best practice)

> Dùng AWS Certificate manager để tạo, lưu trữ, và gán encrytion kéy sai vì ACM chuyên quản lí cert (SSL/TLS) cho https không phải encyption key cho dữ liệu ứng dụng, ACM không hỗ trỡ khóa đối xứng hay envelop encryption dùng sai sẽ dẫn đến không scalable cho dev mã hóa data

> Dùng IAM policy để giới hạn truy cập sai vì đây chỉ là kiểm soát truy cập, không tọ hạ tầng quản lý khóa

# Một công ty cần giữ dữ liệu giao dịch của người dùng tron một amazon DynamoDB, Côn ty phải giữ dữ liệu trong  năm đây là giải pháp tối ưu vận hành nhất cho requiremnt này 

> Suy nghĩ là dùng một dịch vụ hỗ trợ lịch backup tự động, với chính sách lưu trữ linh hoạt, và tuân thủ các audit --> sử dụng AWS Backup để tạo một backup schedule và chính sách lưu trữ cho table 

> Dùng Dynamo DB pooint in memory không hợp lí vì PITR của DynamoDb cho phép bảo vệ dữ liệu khỏi ghi đè hoặc xóa, cho phép rollback về bát kì thời gian nào, nhưng thời gian lưu trữ giữ liệu chỉ tối đa 35 này 

> Thủ công thì không hợp lí lắm

> Dùng EventBridge + Lambda, có thể hoạt động nhưng mà phải kiểm thử, code, xử lí, .... thì việc này k tối ưu vận hành lắm

# Một công ty chuẩn bị lưu trữ dữ liệu bí mật trên S3. Với nhiều lí do phức tạp, dữ liệu phải mã hóa toàn bọ. Encryption key sử dụng phải được log cho mục đích audit. Key phải roateed mọi năm. Đâu là solution tối ưu vận hành nhất

>  Dùng SSE-C, với SSE-C, công ty phải tự tạo, lưu trữ, cung cấp và luân chuyển khóa cho mỗi yêu cầu tải lên, xuống S3. AWS không lưu trữ khóa của người truy cập, do dó khoogn thể cung cấp log chi tiết cho việc kiểm toán. yêu cầu nhiều effort vận hành --> Sai

> SSE-S3 sử dụng các khóa mã hóa do chính dịch vụ S3 tự tạo và quản lý hoàn toàn. Dù rất dễ dùng, nhưng không cung cấp các bản ghi kiểm toán. Khoongg thể chứng minh rằng kieermn toán thấy khóa như nào 

> SSE-KMS với luân chuyển thủ công, luận chuyển thủ công k phải giải háp tói ưu vận hành nhất

> DÙng SSE-KMS với luận chuyển tự động , hỗ trợ mọi thao tác sử dụng khóa cho biết ai dùng, giải mã, mã hóa theo file nào đều có log chi tiets, Luận chuyển tự động mỗi năm, hỗ trợ tính anwng automatic key rotation 

# Một công ty muốn chạy container app trên AWS Cloud, ứng dụng stateless và có thể chiuuj được sự gián đoạn hạ tầng cơ bản. Công ty cần giải pháp tối thiểu chi phí và chi phí vận hành

--> Suy nghĩ khoogn nên dùng EC2 vì sẽ phải tự cấu hình máy chủ, không tối ưu chi phí vận hành, dùng on-demand thườg là cái đắt nhất --> loại đc A, C, D

--> CHọn mù B: Dùng spot instances trong Amazon Elastic Krberetes Service quản lí node group
>> spot instance thì tối ưu chi phí, thích hợp cho việc ứng dụng stateless và có thể chịu được gián đoạn hạ tầng tạm thời (có thể tiết kieemkj lên đến 90% chi phí vs AWS)
>> Amazon EKS managed node group đáp ứng yêu cầu giảm gánh nặng vận hành khi chạy container, AWS sẽ tự động hóa việc cung cấp, cập nhật hệ điều hành, và quản lý vòng đời các máy chủ EC2 trong cụm Kubernestea